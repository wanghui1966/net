阅读《Unix网络编程》，测试代码

各种客户端服务器模型，仅限于tcp

1. 所有的客户端程序都是一样的，与服务器建立链接后，每隔3秒向服务器发送一段数据，并将服务器回复的数据打印
2. 头文件和客户端程序放在common/目录下
3. 可执行文件放在bin/目录下
4. 日志文件放在log/目录下

# 1. 并发服务器，为每个客户请求fork一个进程
fork目录下，为每一个客户端请求fork一个进程单独处理

# 2. select
select目录下

# 3. poll
poll目录下

# 4. thread
thread目录下，为每一个客户端请求创建一个线程单独处理
