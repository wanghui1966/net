阅读《Unix网络编程》，测试代码

unix域协议测试

1. 客户端程序与服务器建立链接后，每隔3秒向服务器发送一段数据，并将服务器回复的数据打印
2. 可执行文件放在bin/目录下
3. 日志文件放在log/目录下
4. 为每一个客户端请求fork一个进程单独处理
5. test_unix_file文件为unix域套接字关联的路径名
6. 其实也可以使用select、poll、thread、epoll技术来完成各种服务器模型，未写。。。
